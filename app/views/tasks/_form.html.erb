<%= form_for task do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :description %>
  <%= f.text_field :description %>
  <%= f.submit ((@task.name.nil? && @task.description.nil?) ? 'create' : 'update') %>
<% end %>
